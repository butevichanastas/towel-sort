
// You should implement your task here.

module.exports = function towelSort (matrix) {
  if(!Array.isArray(matrix) || matrix.length === 0)
  return [];
  else 
  (
    const evenArray = matrix.filter((item, index) => index % 2)
  )

}
